h1 HELLO #{user.name}
br
img(src=user.avatar)
br
form(action='/user', method='POST')
 button LOG OUT
ul
  each post in posts
     li
       span=post.content
a(href='/user/history') HISTORY
br
a(href='/user/friends') FRIENDS

